machine:
  environment:
    PYENV_VERSION: 3.6.4 2.7.14
    TOXENV: py36,py27
  post:
    - for i in $PYENV_VERSION ; do pyenv install -s $i ; done
test:
  override:
    - tox -v
  post:
    - codecov
